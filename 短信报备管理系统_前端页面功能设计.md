# 短信报备管理系统前端页面功能设计文档

## 1. 文档背景

当前系统定位为一个面向短信端口报备、资质资料维护、端口资源管理、三方 API 数据接入展示的管理平台。

本阶段重点聚焦 **前端页面功能设计**，暂不深入后端表结构、权限模型、接口实现和复杂业务流转。

系统主要分为四大模块：

1. 工作台
2. 资质管理 / 报备管理
3. 端口管理
4. 三方 API 接入管理

参考 Excel《三大运营商短信端口字段整合清单》，当前整合后字段约 71 个，字段覆盖基础信息、端口信息、企业信息、责任人信息、经办人信息、授权信息、业务信息、签名信息、模板信息、引流信息、附件信息等。

---

## 2. 设计原则

### 2.1 页面按业务流程划分，不按字段分组堆菜单

不建议把所有字段分组直接做成多个一级菜单，例如“基础信息”“企业信息”“责任人信息”“附件信息”等。这样会导致入口过多，用户维护一条报备记录时需要频繁跳转。

推荐方式：

```text
一级菜单按业务模块划分；
详情页 / 编辑页内部再按字段分组折叠展示。
```

### 2.2 报备记录是系统主线

系统的核心对象应该是“报备记录”。

端口、企业、责任人、附件、模板、API 接入数据等，最终都应该服务于报备记录的创建、维护、查询、导入和导出。

### 2.3 列表只展示核心字段，详情页展示完整字段

列表页字段过多会影响可读性。建议列表页只展示高频检索和判断状态所需字段，完整字段放到详情页和编辑页中。

### 2.4 运营商差异通过模板解决

三大运营商字段要求不完全一致，不建议为每个运营商单独做一套完全不同的页面。

推荐方式：

```text
统一字段模型 + 运营商字段模板 + 动态展示 / 导出字段控制
```

### 2.5 导入导出任务化

导入导出不要只做成简单按钮。由于涉及运营商模板、字段选择、端口组合、校验结果，建议做成独立任务页面。

---

## 3. 字段分组参考

根据整合字段清单，字段大致可以分为以下几组：

| 字段分组 | 字段数量 | 示例字段 |
|---|---:|---|
| 基础信息 | 7 | 运营商、操作类型、报送单位、来源文件、导入批次、当前状态 |
| 端口信息 | 6 | 主端口号、子端口号、码号使用范围、端口类型、端口入网时间 |
| 区域信息 | 3 | 接入省、接入地市、所属地区 |
| 企业信息 | 6 | 企业名称、单位证件类型、单位证件号码、客户类型、集团编码 |
| 责任人信息 | 5 | 责任人姓名、责任人证件类型、责任人证件号码、责任人手机号 |
| 经办人信息 | 5 | 经办人姓名、经办人证件类型、经办人证件号码、经办人手机号 |
| 授权信息 | 5 | 是否具有授权书、授权开始日期、授权结束日期、授权书附件 |
| 业务信息 | 7 | 业务属性、业务类型、业务细类、具体用途、黑白名单类型 |
| 签名信息 | 5 | 短信签名、签名类型、是否签名校验、签名举证附件 |
| 机房信息 | 3 | 运营商接入机房及设备、企业接入机房及设备 |
| 模板信息 | 4 | 短信模板内容、模板参数类型、模板参数长度 |
| 引流信息 | 7 | 引流内容、引流号码、链接类型、链接地址、引流举证附件 |
| 附件信息 | 7 | 单位证件附件、身份证正反面、现场照片、端口审核表 |
| 安全信息 | 1 | 身份信息三要素秘钥 |

页面上不需要把这些都做成菜单，建议放在报备详情 / 编辑页面的分组表单中。

---

## 4. 总体菜单设计

推荐菜单结构如下：

```text
短信报备管理系统
├── 工作台
├── 资质管理 / 报备管理
│   ├── 报备列表
│   ├── 导入任务
│   ├── 导出任务
│   └── 字段模板
├── 端口管理
│   ├── 主端口管理
│   ├── 子端口管理
│   └── 端口关联记录
└── 三方 API 接入管理
    ├── API 接入配置
    ├── 接入数据展示
    └── 接入日志
```

如果第一阶段想进一步精简，可以先做：

```text
工作台
报备管理
端口管理
API 接入管理
```

导入任务、导出任务、字段模板可以作为报备管理下面的 Tab 或二级页面。

---

## 5. 工作台页面设计

### 5.1 页面定位

工作台用于展示系统概览、近期变化、待处理事项和简单趋势。

工作台不承担复杂编辑能力，主要用于让用户快速了解当前报备数据状态。

### 5.2 页面结构

```text
顶部统计卡片
报备趋势图表
运营商分布图表
待处理事项
最近变更记录
```

### 5.3 顶部统计卡片

建议展示 4 到 6 个卡片：

| 卡片名称 | 说明 |
|---|---|
| 报备总数 | 当前系统内全部报备记录数量 |
| 本月新增 | 本月新增报备数量 |
| 本月变更 | 本月发生变更的报备数量 |
| 待完善资料 | 字段或附件缺失的记录数量 |
| 授权即将到期 | 授权结束日期 30 天内到期的记录 |
| 已关联端口 | 已绑定主端口 / 子端口的记录数量 |

### 5.4 趋势图表

建议先做两个简单图表：

1. 近 7 / 30 天报备新增趋势
2. 按运营商统计报备数量

### 5.5 待处理事项

建议展示：

| 待处理类型 | 说明 |
|---|---|
| 未关联端口 | 报备记录没有绑定主端口或子端口 |
| 附件缺失 | 必要附件未上传 |
| 授权即将到期 | 授权结束日期临近 |
| 字段校验失败 | 导入或 API 接入数据存在格式问题 |
| API 接入失败 | 最近 API 同步失败的数据 |

### 5.6 最近变更记录

字段建议：

| 字段 | 说明 |
|---|---|
| 变更时间 | 操作发生时间 |
| 操作类型 | 新增、编辑、导入、导出、状态变更等 |
| 企业名称 | 关联企业 |
| 主端口号 | 关联主端口 |
| 子端口号 | 关联子端口 |
| 运营商 | 移动 / 联通 / 电信 |
| 操作人 | 当前操作用户 |
| 变更说明 | 简要说明 |

---

## 6. 资质管理 / 报备管理页面设计

资质管理是系统核心模块，用于维护报备信息。

### 6.1 报备列表页

#### 页面功能

1. 查询报备记录
2. 新增报备记录
3. 编辑报备记录
4. 查看报备详情
5. 关联端口
6. 批量导入
7. 批量导出
8. 查看变更记录

#### 列表字段

建议列表只展示核心字段：

| 字段 | 说明 |
|---|---|
| 报备编号 | 系统内部编号 |
| 运营商 | 移动 / 联通 / 电信 |
| 企业名称 | 报备企业 |
| 主端口号 | 关联主端口 |
| 子端口号 | 关联子端口 |
| 短信签名 | 当前报备签名 |
| 业务类型 | 通知、营销、验证码等 |
| 接入省 | 省份 |
| 接入地市 | 地市 |
| 当前状态 | 草稿、已报备、变更中、停用等 |
| 授权结束日期 | 用于判断是否即将到期 |
| 更新时间 | 最近维护时间 |
| 操作 | 查看、编辑、导出等 |

#### 搜索条件

基础搜索：

```text
企业名称
主端口号
子端口号
短信签名
运营商
当前状态
```

高级搜索：

```text
统一社会信用代码
业务类型
接入省 / 地市
授权到期时间
创建时间
更新时间
导入批次
```

高级搜索建议默认折叠。

#### 操作按钮

单条操作：

```text
查看
编辑
复制新增
关联端口
导出
变更记录
```

批量操作：

```text
批量导出
批量修改状态
批量关联端口
批量删除 / 作废
```

---

### 6.2 报备新增 / 编辑页

#### 页面布局

推荐采用：

```text
左侧锚点导航 + 右侧分组表单
```

或者：

```text
顶部 Tab + 分组表单
```

更推荐左侧锚点导航，因为字段较多，页面滚动时用户更容易定位。

#### 表单分组

建议分组如下：

```text
基础信息
端口信息
区域信息
企业信息
责任人信息
经办人信息
授权信息
业务信息
签名信息
机房信息
模板信息
引流信息
附件信息
运营商扩展字段
```

#### 基础信息

| 字段 | 控件建议 |
|---|---|
| 运营商 | 单选 / 下拉 |
| 操作类型 | 下拉 |
| 报送单位 | 输入框 |
| 来源文件 | 只读展示 |
| 导入批次 | 只读展示 |
| 当前状态 | 下拉 / 状态标签 |

#### 端口信息

| 字段 | 控件建议 |
|---|---|
| 主端口号 | 下拉选择 / 输入搜索 |
| 子端口号 | 下拉选择 / 输入搜索 |
| 码号使用范围 | 输入框 |
| 端口类型 | 下拉 |
| 端口入网时间 | 日期选择 |
| 是否允许自行扩展 | 单选 |

端口字段建议支持从“端口管理”中选择，而不是完全手输。

#### 企业信息

| 字段 | 控件建议 |
|---|---|
| 企业名称 | 输入框 |
| 单位证件类型 | 下拉 |
| 单位证件号码 | 输入框 |
| 客户类型 | 下拉 |
| 集团编码 | 输入框 |
| APP / 平台名称 | 输入框 |

#### 责任人信息 / 经办人信息

这两组字段结构类似，建议做成统一组件。

| 字段 | 控件建议 |
|---|---|
| 姓名 | 输入框 |
| 证件类型 | 下拉 |
| 证件号码 | 输入框 |
| 证件地址 | 输入框 |
| 手机号 | 输入框 |

#### 授权信息

| 字段 | 控件建议 |
|---|---|
| 是否具有授权书 | 单选 |
| 授权开始日期 | 日期选择 |
| 授权结束日期 | 日期选择 |
| 授权书附件 | 文件上传 |
| 协议合同附件 | 文件上传 |

#### 业务信息

| 字段 | 控件建议 |
|---|---|
| 业务属性 | 下拉 |
| 业务类型 | 下拉 |
| 业务细类 | 下拉 / 输入框 |
| 运营商原始业务分类 | 输入框 / 只读映射 |
| 具体用途 | 文本域 |
| 是否绿色通道 | 单选 |
| 黑白名单类型 | 下拉 |

#### 签名信息

| 字段 | 控件建议 |
|---|---|
| 短信签名 | 输入框 |
| 签名类型 / 来源 | 下拉 |
| 是否签名校验 | 单选 |
| 是否网关签名 | 单选 |
| 签名举证附件 | 文件上传 |

#### 模板信息

模板信息建议做成子表，而不是普通字段堆叠。

| 字段 | 控件建议 |
|---|---|
| 短信模板内容 | 文本域 |
| 模板是否包含变量 | 单选 |
| 模板参数类型 | 输入框 / 下拉 |
| 模板参数长度 | 数字输入 |

如果一个报备记录可能对应多个模板，前端应支持“新增一条模板”。

#### 引流信息

引流信息也建议做成子表。

| 字段 | 控件建议 |
|---|---|
| 引流内容 | 输入框 / 文本域 |
| 引流号码类型 | 下拉 |
| 引流号码 | 输入框 |
| 引流号码用途 | 输入框 |
| 链接类型 | 下拉 |
| 链接地址 | 输入框 |
| 引流举证附件 | 文件上传 |

#### 附件信息

附件建议独立成一个附件上传区。

| 附件类型 | 控件建议 |
|---|---|
| 单位证件附件 | 文件上传 |
| 责任人身份证正面 | 文件上传 |
| 责任人身份证反面 | 文件上传 |
| 经办人身份证正面 | 文件上传 |
| 经办人身份证反面 | 文件上传 |
| 经办人现场照片 | 文件上传 |
| 端口审核表 | 文件上传 |

附件区建议展示上传状态：

```text
未上传
已上传
缺失
格式异常
```

---

### 6.3 报备详情页

详情页用于查看完整报备资料，建议分 Tab：

```text
基础信息
端口与企业信息
联系人与授权信息
业务与签名信息
模板 / 引流信息
附件信息
变更记录
```

详情页重点是“可读性”，不建议所有字段都以编辑表单形式展示。

---

## 7. 导入任务页面设计

### 7.1 页面定位

导入任务用于批量导入三大运营商报备文件，并完成字段识别、字段映射、数据校验和导入结果展示。

### 7.2 导入流程

建议采用步骤条：

```text
1. 选择运营商
2. 上传 Excel
3. 字段映射预览
4. 数据校验
5. 确认导入
6. 导入结果
```

### 7.3 第一步：选择运营商

字段：

| 字段 | 说明 |
|---|---|
| 运营商 | 移动 / 联通 / 电信 |
| 导入类型 | 新增 / 变更 / 注销 |
| 数据来源 | 手工上传 / API 数据 / 其他 |

### 7.4 第二步：上传 Excel

功能：

```text
上传 Excel 文件
下载导入模板
展示文件名称、大小、上传人、上传时间
```

### 7.5 第三步：字段映射预览

表格字段：

| 字段 | 说明 |
|---|---|
| 原始字段 | Excel 中的字段名 |
| 统一字段 | 系统统一字段名称 |
| 字段分组 | 基础信息、端口信息等 |
| 是否必填 | 根据运营商模板判断 |
| 识别状态 | 已识别 / 未识别 / 冲突 |
| 备注 | 映射说明 |

### 7.6 第四步：数据校验

校验结果卡片：

```text
总行数
可导入数量
警告数量
错误数量
```

校验列表字段：

| 字段 | 说明 |
|---|---|
| 行号 | Excel 行号 |
| 企业名称 | 当前行企业 |
| 主端口号 | 当前行主端口 |
| 子端口号 | 当前行子端口 |
| 错误字段 | 校验失败字段 |
| 错误原因 | 失败说明 |
| 处理建议 | 修改建议 |

常见错误：

```text
主端口号为空
子端口号为空
企业证件号码格式异常
手机号格式异常
授权结束日期早于开始日期
短信签名格式异常
必要附件缺失
```

### 7.7 第五步：确认导入

确认页展示：

```text
本次导入运营商
导入类型
文件名称
总行数
可导入数量
错误数量
是否覆盖已有记录
```

### 7.8 第六步：导入结果

结果页展示：

```text
导入成功数量
导入失败数量
跳过数量
生成的导入批次号
下载错误明细
查看本次导入记录
```

---

## 8. 导出任务页面设计

### 8.1 页面定位

导出任务用于按照运营商、字段分组、字段模板、端口组合规则导出报备数据。

该页面是本系统的重点页面之一。

### 8.2 导出流程

建议采用步骤条：

```text
1. 选择数据范围
2. 选择导出模板
3. 选择字段范围
4. 配置端口组合方式
5. 预览导出结果
6. 确认导出
```

### 8.3 选择数据范围

筛选条件：

| 条件 | 说明 |
|---|---|
| 运营商 | 移动 / 联通 / 电信 / 全部 |
| 当前状态 | 草稿 / 已报备 / 变更中 / 停用 |
| 企业名称 | 模糊搜索 |
| 接入省 / 地市 | 区域筛选 |
| 业务类型 | 类型筛选 |
| 创建时间 | 时间范围 |
| 授权到期时间 | 时间范围 |
| 导入批次 | 按批次导出 |

### 8.4 选择导出模板

模板类型：

| 模板 | 说明 |
|---|---|
| 中国移动模板 | 按移动字段要求导出 |
| 中国联通模板 | 按联通字段要求导出 |
| 中国电信模板 | 按电信字段要求导出 |
| 自定义字段模板 | 用户自行选择字段 |

模板卡片示例：

```text
中国移动模板
字段数量：34
适用场景：移动报备提交
```

```text
中国联通模板
字段数量：24
适用场景：联通报备提交
```

```text
中国电信模板
字段数量：52
适用场景：电信报备提交
```

### 8.5 选择字段范围

字段选择建议按分组折叠：

```text
基础信息
端口信息
区域信息
企业信息
责任人信息
经办人信息
授权信息
业务信息
签名信息
机房信息
模板信息
引流信息
附件信息
安全信息
```

每个分组支持：

```text
全选
取消全选
展开 / 收起
```

字段选择示例：

```text
端口信息
[x] 主端口号
[x] 子端口号
[x] 码号使用范围
[x] 端口类型
[ ] 端口入网时间
[ ] 是否允许自行扩展
```

### 8.6 配置端口组合方式

由于导出可能涉及随机组合端口管理中的端口，建议单独做一个配置区域。

端口来源：

| 方式 | 说明 |
|---|---|
| 使用报备已绑定端口 | 默认方式，直接导出当前报备关联端口 |
| 从端口管理中随机分配 | 从端口资源池随机选择端口 |
| 按运营商随机分配 | 只从当前运营商端口池选择 |
| 按省份 / 地市随机分配 | 只从指定区域端口池选择 |
| 手动选择端口 | 用户手动指定端口 |

随机规则：

```text
是否允许重复使用
是否只选择空闲端口
每条报备分配几个子端口
是否保持主端口一致
是否按运营商匹配
是否按区域匹配
```

### 8.7 预览导出结果

导出前必须提供预览，尤其是涉及随机端口组合时。

预览字段：

| 字段 | 说明 |
|---|---|
| 企业名称 | 报备企业 |
| 运营商 | 当前运营商 |
| 原主端口 | 原绑定主端口 |
| 原子端口 | 原绑定子端口 |
| 导出主端口 | 实际导出的主端口 |
| 导出子端口 | 实际导出的子端口 |
| 字段数量 | 本次导出字段数量 |
| 校验状态 | 是否可导出 |

### 8.8 导出结果

导出完成后展示：

```text
导出任务编号
导出模板
导出数据量
导出字段数量
创建人
创建时间
下载文件
```

---

## 9. 字段模板页面设计

### 9.1 页面定位

字段模板用于管理不同运营商、不同场景下的导入 / 导出字段范围。

### 9.2 字段模板列表

字段：

| 字段 | 说明 |
|---|---|
| 模板名称 | 例如：中国移动报备模板 |
| 运营商 | 移动 / 联通 / 电信 |
| 模板类型 | 导入 / 导出 |
| 字段数量 | 当前模板包含字段数 |
| 是否默认 | 是否为默认模板 |
| 更新时间 | 最近维护时间 |
| 操作 | 查看、编辑、复制、停用 |

### 9.3 字段模板编辑

功能：

```text
选择运营商
选择模板类型
按分组选择字段
设置字段顺序
设置字段别名
设置是否必填
设置默认值
```

字段别名很重要，因为统一字段和运营商原始字段可能不一致。

示例：

| 统一字段 | 移动字段名 | 联通字段名 | 电信字段名 |
|---|---|---|---|
| 主端口号 | 局数据端口 | 短信主端口号 | 主端口号 |
| 子端口号 | 企业代码 | 短信子端口号 | 子端口号 |

---

## 10. 端口管理页面设计

端口管理是报备系统的资源池，用于维护主端口、子端口以及端口和报备记录之间的关系。

### 10.1 主端口管理

#### 列表字段

| 字段 | 说明 |
|---|---|
| 主端口号 | 主码号 |
| 运营商 | 移动 / 联通 / 电信 |
| 码号使用范围 | 使用范围说明 |
| 接入省 | 省份 |
| 接入地市 | 地市 |
| 端口类型 | 端口分类 |
| 状态 | 空闲、使用中、停用、异常 |
| 已关联子端口数 | 当前子端口数量 |
| 创建时间 | 创建时间 |
| 操作 | 查看、编辑、新增子端口等 |

#### 搜索条件

```text
主端口号
运营商
接入省 / 地市
端口类型
状态
```

#### 操作

```text
查看
编辑
新增子端口
查看关联报备
停用
```

---

### 10.2 子端口管理

#### 列表字段

| 字段 | 说明 |
|---|---|
| 子端口号 | 子码号 |
| 所属主端口 | 关联主端口 |
| 运营商 | 移动 / 联通 / 电信 |
| 企业名称 | 当前关联企业 |
| 短信签名 | 当前关联签名 |
| 业务类型 | 当前业务类型 |
| 状态 | 空闲、已分配、已报备、停用 |
| 关联报备编号 | 关联报备记录 |
| 更新时间 | 最近更新时间 |
| 操作 | 查看、编辑、解绑等 |

#### 子端口状态

| 状态 | 说明 |
|---|---|
| 空闲 | 尚未分配 |
| 已分配 | 已被分配但未完成报备 |
| 已报备 | 已和报备记录绑定 |
| 停用 | 不再使用 |

---

### 10.3 端口详情页

详情页建议分 Tab：

```text
基础信息
子端口列表
关联报备
使用记录
变更记录
```

### 10.4 端口关联记录

该页面用于查看端口和报备记录之间的历史关系。

列表字段：

| 字段 | 说明 |
|---|---|
| 主端口号 | 主端口 |
| 子端口号 | 子端口 |
| 企业名称 | 关联企业 |
| 报备编号 | 关联报备 |
| 关联时间 | 开始使用时间 |
| 解绑时间 | 结束使用时间 |
| 当前状态 | 当前是否有效 |
| 操作人 | 维护人 |

---

## 11. 三方 API 接入管理页面设计

当前阶段仅做展示和配置，后期再拓展自动入库、字段映射和任务调度。

### 11.1 API 接入配置列表

字段：

| 字段 | 说明 |
|---|---|
| 接口名称 | API 名称 |
| 接口来源 | 第三方系统名称 |
| 接口地址 | API URL |
| 请求方式 | GET / POST |
| 认证方式 | Token / AppKey / Basic Auth 等 |
| 启用状态 | 启用 / 停用 |
| 最近同步时间 | 最近一次接入时间 |
| 最近同步结果 | 成功 / 失败 |
| 操作 | 查看、编辑、测试连接等 |

操作：

```text
查看
编辑
测试连接
启用 / 停用
查看接入数据
查看日志
```

---

### 11.2 API 配置新增 / 编辑

字段：

| 字段 | 控件建议 |
|---|---|
| 接口名称 | 输入框 |
| 接口地址 | 输入框 |
| 请求方式 | 下拉 |
| 认证方式 | 下拉 |
| 请求头配置 | Key-Value 表格 |
| 请求参数配置 | Key-Value 表格 |
| 数据类型 | 报备数据 / 端口数据 / 企业数据 |
| 同步方式 | 手动 / 定时 |
| 是否启用 | 开关 |

当前阶段可以先不做复杂字段映射，只展示配置能力。

---

### 11.3 API 接入数据展示

字段：

| 字段 | 说明 |
|---|---|
| 接入时间 | 数据进入系统时间 |
| 接口名称 | 来源接口 |
| 数据类型 | 报备 / 端口 / 企业 |
| 企业名称 | 接入数据中的企业 |
| 主端口号 | 接入数据中的主端口 |
| 子端口号 | 接入数据中的子端口 |
| 运营商 | 移动 / 联通 / 电信 |
| 处理状态 | 待处理、已入库、校验失败、已忽略 |
| 错误原因 | 失败说明 |

### 11.4 API 接入日志

字段：

| 字段 | 说明 |
|---|---|
| 请求时间 | API 请求时间 |
| 接口名称 | 当前接口 |
| 请求地址 | 请求 URL |
| 响应状态码 | HTTP 状态码 |
| 请求耗时 | 接口耗时 |
| 结果 | 成功 / 失败 |
| 错误信息 | 失败原因 |

---

## 12. 推荐开发优先级

### 第一阶段：基础可用

目标：能看、能录、能查。

```text
1. 工作台
2. 报备列表
3. 报备新增 / 编辑
4. 报备详情
5. 主端口管理
6. 子端口管理
7. API 接入数据展示
```

### 第二阶段：批量处理

目标：支持批量导入和导出。

```text
1. 导入任务
2. 导出任务
3. 字段模板
4. 导入结果校验
5. 导出字段选择
```

### 第三阶段：增强能力

目标：提升自动化和复杂场景支持。

```text
1. 端口随机组合导出
2. API 接入配置
3. API 字段映射
4. 操作日志
5. 变更记录
6. 附件完整性校验
```

---

## 13. 前端页面路由建议

如果使用 React / Vue 管理后台，可以参考以下路由结构：

```text
/dashboard

/records
/records/create
/records/:id/detail
/records/:id/edit
/records/import-tasks
/records/export-tasks
/records/field-templates

/ports/main
/ports/sub
/ports/relations
/ports/:id/detail

/api-connections/configs
/api-connections/data
/api-connections/logs
```

---

## 14. 页面组件建议

可抽象的通用组件：

| 组件 | 用途 |
|---|---|
| SearchForm | 列表搜索区域 |
| DataTable | 统一表格 |
| StatusTag | 状态标签 |
| FieldGroupForm | 分组表单 |
| AnchorFormLayout | 左侧锚点 + 右侧表单布局 |
| FileUploadGroup | 附件上传区 |
| ImportSteps | 导入步骤条 |
| ExportSteps | 导出步骤条 |
| FieldSelector | 字段分组选择器 |
| PortSelector | 主端口 / 子端口选择器 |
| ChangeLogTable | 变更记录表 |
| ApiConfigForm | API 配置表单 |

---

## 15. 关键交互说明

### 15.1 报备编辑时的字段展示

根据运营商动态展示字段：

```text
选择移动：展示移动需要的字段
选择联通：展示联通需要的字段
选择电信：展示电信需要的字段
```

但底层仍然保存到统一字段模型中。

### 15.2 导出字段选择

导出时支持：

```text
按运营商模板导出
按字段分组导出
按自定义字段导出
按当前筛选结果导出
按选中数据导出
```

### 15.3 端口随机组合

涉及随机组合时，必须增加导出预览，避免用户无法确认导出结果。

建议流程：

```text
选择数据范围
选择端口随机规则
生成预览
确认无误后导出
```

### 15.4 附件展示

附件不要散落在各个表单位置，建议统一在附件区展示，同时在对应字段旁边保留快捷入口。

---

## 16. 总结

该系统的页面设计重点不是简单展示全部字段，而是围绕“报备记录”设计一套清晰的维护流程。

推荐核心思路：

```text
报备管理作为主流程
端口管理作为资源池
字段模板解决运营商差异
导入导出任务化
API 接入先展示，后续再做自动入库和字段映射
```

第一阶段建议优先完成以下页面：

```text
工作台
报备列表
报备新增 / 编辑
报备详情
端口管理列表
API 接入数据展示
```

这几个页面完成后，系统主骨架就已经清晰，后续再逐步补充导入、导出、字段模板、端口随机组合、API 接入配置等增强能力。

