# SMS 报备管理平台 - 工作日志汇总

> 范围：最近 30 条提交（2026-07-03 ~ 2026-07-05）

## 1. 新增

### 1.1 报备管理整体改造：用「报备任务批量导出」替代「单条报备录入」

将原有逐条录入报备记录的工作流替换为基于资质池/端口池的批量 Excel 导出方式，提升报备批量产出效率。

主要支持：

1. 新增「报备任务」概念，每次导出操作都会创建一条任务记录，可在报备管理列表中查看与重新下载
2. 提供三步式向导：选择资质 → 配置导出参数 → 生成并下载 Excel
3. 自动按规则从端口池随机挑选可用端口，与所选资质做笛卡尔积后分组写入 Excel
4. 生成的文件存入对象存储，列表页支持随时通过预签名链接重新下载
5. 同时清理了旧的报备记录录入、详情、编辑、查询等所有相关页面与接口

### 1.2 资质管理页新增 Excel 批量导入能力

资质页支持通过模板批量录入资质信息，免去逐条手工填写。

主要支持：

1. 资质页新增「批量导入」「下载导入模板」按钮
2. 模板包含 15 个资质标准字段，下载即为可直接填写的 Excel
3. 上传 Excel 后自动校验并写入数据库，导入失败会给出错误提示
4. 文件上传走标准表单方式，避免 multipart 边界丢失

### 1.3 端口信息页新增 Excel 批量导入能力

端口信息页与资质页对齐，提供模板下载 + 批量导入能力。

主要支持：

1. 端口信息页新增「批量导入」「下载导入模板」按钮
2. 后端新增端口信息 Excel 导入接口与模板下载接口
3. 对异常日期格式等给出明确报错提示，便于用户修正文件
4. 复用统一的导入对话框组件，体验与资质导入一致

### 1.4 新增统一的「导入对话框」可复用组件

为资质、端口等需要 Excel 导入的页面提供统一样式与交互的对话框组件。

主要支持：

1. 内置「下载模板」「选择文件」「提交导入」三步操作
2. 关闭对话框时自动重置内部状态，避免下次打开时残留
3. 暴露通用回调，资质、端口页直接复用即可

### 1.5 报备记录详情页新增「附件」页签完整能力

详情页支持附件上传、预览、下载、删除，便于将报备相关证明材料集中管理。

主要支持：

1. 详情页新增「附件」Tab，集中展示该报备相关所有文件
2. 支持文件列表展示、图片缩略图预览、单文件下载与删除
3. 表单页的附件信息区域改为引导用户去详情页上传，避免重复上传逻辑
4. 删除文件接口补齐登录鉴权，未登录用户无法调用

### 1.6 接入 MinIO 对象存储

后端服务接入 MinIO 作为文件存储后端，支撑报备任务 Excel 与各类附件的统一存储。

主要支持：

1. 开发环境 docker-compose 接入 MinIO 服务与环境变量
2. 生产部署 docker-compose 也补齐 MinIO 服务，保证环境一致
3. 报备任务生成的 Excel 文件、附件均通过 MinIO 存储
4. 提供预签名 URL 下载链接，浏览器直接跟随重定向完成下载

### 1.7 管理后台新增「操作日志」页面

管理员可在后台查看系统内重要操作的记录，便于审计追溯。

主要支持：

1. 后端补齐操作日志的采集服务与查询接口
2. 前端新增操作日志列表页，支持按条件查询

### 1.8 管理后台新增「导出分组」页面

新增导出分组管理页面，便于对报备导出任务按业务分组进行组织与查询。

主要支持：

1. 前端新增导出分组列表与详情展示
2. 支持新建导出分组对话框，按需配置分组条件

### 1.9 管理后台新增「港口管理」主港口 / 子港口页面

将港口相关信息从单一列表升级为主港口 + 子港口两级管理。

主要支持：

1. 新增主港口管理页，含主港口新增、编辑对话框
2. 新增子港口管理页，含子港口新增、编辑对话框
3. 侧边栏加入对应菜单项，路径与路由自动注册

### 1.10 仪表盘新增「资质即将到期」列表

首页 Dashboard 新增资质到期提醒视图，便于提前安排资质续期。

主要支持：

1. 首页新增「即将到期资质」组件，列出近期将到期的资质
2. 配合其他仪表盘组件（统计卡、趋势图、饼图等）整体优化

### 1.11 报备任务列表支持重新下载已生成的 Excel

报备任务支持任务记录与下载分离，任务完成后可随时回到列表重新下载。

主要支持：

1. 报备任务列表新增下载入口
2. 下载使用对象存储直链 + 浏览器跟随重定向，无需前端二次拉取

## 2. 优化

### 2.1 资质录入对话框按字段分组重构

资质录入表单字段较多，按业务维度重新分组并加分组标题，让录入体验更清晰。

### 2.2 多个核心页面统一顶部功能区布局

工作台、报备管理、新建报备三个页面此前缺少与其他页面一致的顶部功能区域（全局搜索、主题切换、用户下拉），本次统一补齐 Header + Main 布局，全站体验一致。

### 2.3 账户设置表单精简

账户设置页面表单进行了整体精简，去除冗余字段，提升填写效率。

### 2.4 镜像构建脚本支持版本标签

部署构建脚本新增版本标签传参支持，便于按版本回溯或回滚生产镜像。

### 2.5 文件下载与缩略图逻辑简化

文件下载与缩略图展示改为基于浏览器原生重定向的直链方式，去除了前端额外的异步拉取逻辑，下载体验更稳定。

## 3. 修复

### 3.1 修复报备任务保存时因 UUID 字段无法 JSON 序列化导致报错

报备任务持久化资质 ID、端口 ID 时使用了 UUID 对象，写入 JSON 列时抛出序列化异常，导致整个报备任务创建失败。

修复后，相关字段统一改为字符串存储，并在写入前显式转换，报备任务可正常创建与查询。

### 3.2 修复港口信息导入 Excel 时遇到非法日期格式直接 500

港口信息导入接口在 Excel 中存在非法日期字符串时直接抛出未捕获异常，整批导入失败且无明确提示。

修复后，对非法日期格式进行捕获并返回友好错误信息，用户可在 Excel 中修正后重新导入。

### 3.3 修复导入对话框关闭后下次打开仍残留上次状态

资质/端口导入对话框关闭时未清空已选文件与错误提示，下次打开仍能看到上次的失败信息。

修复后，对话框关闭时统一重置内部状态，每次打开均为干净初始状态。

### 3.4 修复批量导入文件上传被服务端拒绝

前端在 FormData 上传时显式设置了 Content-Type，导致 multipart 边界参数丢失，服务端无法解析文件体，资质和端口的导入上传一直失败。

修复后，删除前端手动设置的 Content-Type，由浏览器自动按 FormData 内容生成正确头部，文件上传恢复正常。

### 3.5 修复资质导入模板下载时中文文件名乱码

资质导入模板下载接口返回的中文文件名在浏览器中显示为乱码。

修复后，文件名按 RFC 5987 规范编码，主流浏览器均能正确显示中文文件名。

### 3.6 修复下拉筛选框值为空字符串时整页崩溃

通用搜索表单中部分下拉选项使用了空字符串作为值，与底层 UI 组件约束冲突，一旦渲染即抛错白屏。

修复后，内部使用占位标识代表「全部」，对外的搜索逻辑保持原行为，下拉框可正常渲染与切换。

### 3.7 修复后端容器重启后 Nginx 仍指向旧 IP 导致 502

生产环境后端容器重启后获得新 IP，Nginx 因启动时一次性解析了后端域名，仍将请求转发到旧 IP，导致整站持续 502 直到 Nginx 重启。

修复后，Nginx 改用 Docker 内置 DNS 解析器，并对后端域名做动态解析，后端重启后 Nginx 自动跟随新 IP，无需手动介入。

### 3.8 修复删除文件接口缺少登录态校验

删除文件接口在某次调整后丢失了登录用户依赖，存在被未登录调用风险。

修复后，删除接口重新挂上登录态校验，未登录用户无法调用。

### 3.9 修复文件下载流程缺少错误处理

文件下载在请求失败时无任何提示，用户不知道是网络问题还是文件本身丢失。

修复后，下载流程补齐错误处理与 useEffect 清理，失败时给出明确反馈。

---

**汇总统计**：新增 11 项、优化 5 项、修复 9 项，覆盖 30 条提交。
